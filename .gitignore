# [INPUT]: 仓库根目录中由包管理器或本地开发产生的临时产物。
# [OUTPUT]: Git 应忽略的根级本地产物规则。
# [POS]: 位于仓库根目录，作为 Kata 仓库根级忽略规则入口。
#
# [PROTOCOL]:
# 1. 一旦根级忽略规则变化，必须同步更新此 Header。
# 2. 更新后必须上浮检查 /README.md 中根目录索引是否依然准确。
node_modules/

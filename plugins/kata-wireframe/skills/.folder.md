<!--
[INPUT]: `/plugins/kata-wireframe/skills` 下 skill 子目录变化。
[OUTPUT]: wireframe skills 层的局部架构摘要与子目录索引。
[POS]: 位于 `/plugins/kata-wireframe/skills`，作为 wireframe 类 skill 集合层锚点。

[PROTOCOL]:
1. 一旦本目录子目录变化，必须同步更新我。
2. 若新增 skill 进入可用状态，需同步回写上层 plugin 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-wireframe/skills

这里汇总 `kata-wireframe` 的 skill 子目录。
当前只预留 `wireframe` 方向的结构骨架。
后续新增 skill 时，必须在这里先登记再实现。

- `.folder.md`：本目录的局部索引与同步锚点。
- `wireframe/`：wireframe skill 的预留目录。

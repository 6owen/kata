<!--
[INPUT]: `/plugins/kata-design/skills` 下 skill 子目录变化。
[OUTPUT]: `kata-design` skills 层的局部架构摘要与子目录索引。
[POS]: 位于 `/plugins/kata-design/skills`，作为设计编排 skill 集合层锚点。

[PROTOCOL]:
1. 一旦本目录子目录变化，必须同步更新我。
2. 若 skill 能力边界变化，需同步回写上层 plugin 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-design/skills

这里汇总 `kata-design` 的所有 skill。
当前维护 `impeccable-integration` 与 `website-to-design-md` 两项能力。
后续若新增设计类 skill，必须先在这里登记职责边界。

- `.folder.md`：本目录的局部索引与同步锚点。
- `impeccable-integration/`：外部 `impeccable` companion plugin 的集成入口。
- `website-to-design-md/`：把实时网站抽取为 `DESIGN.md` 与预览 HTML 的设计分析 skill。

<!--
[INPUT]: `impeccable-integration` skill 的说明文件、参考资料与集成边界变更。
[OUTPUT]: `impeccable-integration` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-design/skills/impeccable-integration`，作为设计 companion 集成 skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若外部 plugin 接入方式、降级策略或职责边界变化，需同步更新 README、SKILL.md 与 references 说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-design/skills/impeccable-integration

这里定义 `kata-design` 对外部 `impeccable` 的集成 skill。
`SKILL.md` 约束 AI 路由策略，`README.md` 解释定位，`references/` 放补充资料。
集成边界调整时，三者必须一起更新。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 skill 简介。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `references/`：补充参考资料目录。

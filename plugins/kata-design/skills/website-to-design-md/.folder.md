<!--
[INPUT]: `website-to-design-md` skill 的说明文件、模板、脚本与 agent 配置变更。
[OUTPUT]: `website-to-design-md` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-design/skills/website-to-design-md`，作为该 skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若技能名称、提取流程或输出契约变化，需同步更新 README、SKILL.md、子目录 `.folder.md` 与上层 skill 索引。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-design/skills/website-to-design-md

这里定义把实时网站转成 `DESIGN.md` 与 HTML 预览的设计抽取 skill。
`SKILL.md` 约束 AI 工作流，`agents/` 提供展示配置，`assets/`、`references/`、`scripts/` 分别承载模板、补充资料与执行脚本。
技能名称或输出结构调整时，目录索引与入口说明必须一起更新。

- `.folder.md`：本目录的局部索引与同步锚点。
- `.gitignore`：忽略 skill 运行产物。
- `README.md`：面向人类的 skill 简介与安装说明。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `agents/`：agent 平台侧展示名与默认 prompt 配置。
- `assets/`：`DESIGN.md` 模板与预览 HTML 模板。
- `references/`：网站阅读与浏览器工具补充说明。
- `scripts/`：浏览器工具检测、证据抽取与预览渲染脚本。

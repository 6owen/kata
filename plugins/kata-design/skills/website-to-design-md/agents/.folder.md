<!--
[INPUT]: `agents/` 内 agent 平台适配元数据变更。
[OUTPUT]: `website-to-design-md/agents` 的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-design/skills/website-to-design-md/agents`，作为 agent 配置目录锚点。

[PROTOCOL]:
1. 一旦本目录文件变化，必须同步更新我。
2. 若展示名、描述或默认 prompt 变化，需同步更新上层 README、SKILL.md 与目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-design/skills/website-to-design-md/agents

这里放置 `website-to-design-md` 的 agent 平台侧元数据。
当前仅维护 OpenAI 适配配置。
展示名与调用提示必须与 skill slug 保持一致。

- `.folder.md`：本目录的局部索引与同步锚点。
- `openai.yaml`：OpenAI agent 展示名、简述与默认 prompt。

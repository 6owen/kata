<!--
[INPUT]: `/plugins/kata-design` 内 plugin 文档、元数据与 skills 结构变更。
[OUTPUT]: `kata-design` plugin 的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-design`，作为设计编排 plugin 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若外部 companion plugin 集成方式变化，需同步更新 README、`plugin.json`、上层 `/plugins/.folder.md` 与根 README。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-design

这里承载设计流程编排 plugin 的根说明、发布元数据与 skills 集合。
本 plugin 负责约束集成方式，不负责复制 `impeccable` 上游实现。
集成边界变化时，必须在这里先明确。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 plugin 定位说明。
- `plugin.json`：JSON 例外文件；提供 `kata-design` 的发布元数据。
- `skills/`：具体 skill 集合目录，细节由子目录 `.folder.md` 继续描述。

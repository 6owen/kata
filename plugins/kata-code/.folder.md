<!--
[INPUT]: `/plugins/kata-code` 内 plugin 文档、元数据与 skills 结构变更。
[OUTPUT]: `kata-code` plugin 的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code`，作为代码工程 plugin 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若 skill 清单、发布元数据或定位变化，需同步更新本目录 README、上层 `/plugins/.folder.md` 与根 README。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code

这里承载代码工程相关 plugin 的根说明、发布元数据与 skills 集合。
当前覆盖 monorepo、工具链、后端模块化与 Vite React starter 基线。
README 负责对人类解释定位，`plugin.json` 负责发布元信息，`skills/` 负责具体能力。
任一 skill 增删或行为转向，都必须先回写这里。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 plugin 定位说明。
- `plugin.json`：JSON 例外文件；提供 `kata-code` 的发布元数据。
- `skills/`：具体 skill 集合目录，细节由子目录 `.folder.md` 继续描述。

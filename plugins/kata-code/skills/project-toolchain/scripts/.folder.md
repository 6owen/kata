<!--
[INPUT]: `project-toolchain` 执行脚本的实现、参数与输出配置变更。
[OUTPUT]: scripts 子目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/project-toolchain/scripts`，作为工具链执行层目录锚点。

[PROTOCOL]:
1. 一旦本目录脚本文件变化，必须同步更新我。
2. 若脚本写入 `package.json`、ESLint 或 hooks 的方式变化，需同步回写上层 skill 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/project-toolchain/scripts

这里承载 `project-toolchain` 的实际执行脚本。
脚本负责安装依赖并补齐目标项目的工具链默认配置，包括 `package.json` 中缺失的 lint/format/fix/up 脚本。
脚本改写面一旦变化，必须同步修正文档索引。

- `.folder.md`：本目录的局部索引与同步锚点。
- `apply-project-toolchain.mjs`：对目标项目注入 Arvinn 工具链约定的执行脚本。

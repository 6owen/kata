<!--
[INPUT]: `project-toolchain` skill 的说明文件、执行脚本与依赖契约变更。
[OUTPUT]: `project-toolchain` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/project-toolchain`，作为工具链接入 skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或脚本行为变化，必须同步更新我。
2. 若依赖包、脚本注入策略或 VS Code 同步流程变化，需同步更新 README、SKILL.md 与脚本 Header。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/project-toolchain

这里定义 pnpm 工具链接入与代码规范化 skill。
说明文件给出行为边界，脚本负责改写 `package.json`、ESLint 与 VS Code 相关配置。
依赖和写入策略一旦变化，必须保持文档与脚本一致。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 skill 简介。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `scripts/`：实际执行脚本目录。

<!--
[INPUT]: `pnpm-monorepo` skill 的定位、使用方式与迁移边界说明。
[OUTPUT]: 面向人类的 pnpm monorepo skill 简介。
[POS]: 位于 /plugins/kata-code/skills/pnpm-monorepo，作为该 skill 的人类入口文档。

[PROTOCOL]:
1. 一旦 skill 行为、迁移策略或输出结构变化，必须同步更新此 Header。
2. 更新后必须上浮检查本目录 `.folder.md`、`SKILL.md` 与 `scripts/` 下脚本 Header 是否依然准确。
-->

# pnpm-monorepo

Set up or migrate projects to a pnpm monorepo with `apps/web` as default app location.

Main entry: `SKILL.md`

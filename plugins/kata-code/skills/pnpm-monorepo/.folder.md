<!--
[INPUT]: `pnpm-monorepo` skill 的说明文件、执行脚本与输出契约变更。
[OUTPUT]: `pnpm-monorepo` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/pnpm-monorepo`，作为 monorepo skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或脚本行为变化，必须同步更新我。
2. 若目录迁移规则、交互确认或生成产物变化，需同步更新 README、SKILL.md 与脚本 Header。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/pnpm-monorepo

这里定义 pnpm monorepo 初始化与迁移 skill。
说明文件描述规则，脚本真正重排项目根结构与 `apps/web` 入口。
迁移策略变动时，必须保证文档与脚本一致。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 skill 简介。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `scripts/`：实际执行脚本目录。

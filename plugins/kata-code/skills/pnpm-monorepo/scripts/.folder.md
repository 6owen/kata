<!--
[INPUT]: `pnpm-monorepo` 执行脚本的实现、参数与迁移行为变更。
[OUTPUT]: scripts 子目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/pnpm-monorepo/scripts`，作为 monorepo 执行层目录锚点。

[PROTOCOL]:
1. 一旦本目录脚本文件变化，必须同步更新我。
2. 若脚本保留目录、迁移策略或交互方式变化，需同步回写上层 skill 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/pnpm-monorepo/scripts

这里承载 `pnpm-monorepo` 的实际执行脚本。
脚本负责初始化工作区，或把单体前端迁移到 `apps/web`。
任何迁移边界变化，都必须在这里和上层同时留痕。

- `.folder.md`：本目录的局部索引与同步锚点。
- `apply-pnpm-monorepo.mjs`：对目标项目建立或迁移 pnpm monorepo 结构的执行脚本。

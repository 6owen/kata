<!--
[INPUT]: `starter-react` skill 的说明文件、执行脚本、参考资料、TanStack Start 路由约定、React companion reference 与目录收敛契约变更。
[OUTPUT]: `starter-react` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/starter-react`，作为 Vite React starter skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若模板来源、脚本行为、TanStack Start 路由约定、样式基线、React companion reference 或迁移边界变化，需同步更新 README、SKILL.md、scripts 与 references 说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/starter-react

这里定义基于 `6owen/starter-react` 的单应用前端 starter skill。
`SKILL.md` 约束 AI 的初始化与迁移流程，`scripts/` 承担 init 执行入口，`references/` 记录 TanStack Start 模板契约与 React companion 使用边界。
模板目录、脚本行为、`src/pages` 的 `route.tsx` / `index.tsx` 约定、依赖基线、外部 React 指南引用或迁移策略调整时，三者必须一起更新。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 skill 简介。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `scripts/`：初始化执行脚本目录。
- `references/`：模板契约与补充参考资料目录。

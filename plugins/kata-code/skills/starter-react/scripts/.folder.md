<!--
[INPUT]: `starter-react` 执行脚本的实现、参数与初始化行为变更。
[OUTPUT]: scripts 子目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/starter-react/scripts`，作为 starter-react 执行层目录锚点。

[PROTOCOL]:
1. 一旦本目录脚本文件变化，必须同步更新我。
2. 若脚本参数、模板来源优先级或复制边界变化，需同步回写上层 skill 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/starter-react/scripts

这里承载 `starter-react` 的实际初始化脚本。
脚本负责创建目标目录、复制模板、写回包名，并可选执行 `pnpm install`。
任何初始化边界变化，都必须在这里和上层同时留痕。

- `.folder.md`：本目录的局部索引与同步锚点。
- `apply-starter-react.mjs`：对目标目录初始化 `starter-react` 基线的执行脚本。

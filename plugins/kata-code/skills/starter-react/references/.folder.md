<!--
[INPUT]: `starter-react` 参考资料文件与模板契约快照变更。
[OUTPUT]: references 子目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/starter-react/references`，作为模板参考资料层目录锚点。

[PROTOCOL]:
1. 一旦本目录文件变化，必须同步更新我。
2. 若参考资料调整了模板边界、页面路由约定、样式基线或迁移契约，需同步回写上层 skill 目录说明。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/starter-react/references

这里存放 `starter-react` 的模板契约与补充参考资料。
参考资料用于帮助 AI 对齐模板结构，不承担正式执行入口职责。
上游模板 README、页面结构或样式约定变化时，必须先回写这里再更新上层 skill。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：参考资料层的人类说明。
- `template-contract.md`：从 `6owen/starter-react` 提炼出的结构与迁移契约。

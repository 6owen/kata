<!--
[INPUT]: `tailwind-iconify` skill 的说明文件、执行脚本与配置改写契约变更。
[OUTPUT]: `tailwind-iconify` 目录的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-code/skills/tailwind-iconify`，作为 Tailwind 图标 workflow skill 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或脚本行为变化，必须同步更新我。
2. 若依赖包、Tailwind 配置写入策略或 CSS 指令注入方式变化，需同步更新 README、SKILL.md 与脚本 Header。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills/tailwind-iconify

这里定义 Tailwind v4 + Iconify 的接入 skill。
说明文件定义边界，脚本负责改写 Tailwind 配置与全局 CSS 入口。
配置写入规则变化时，必须把差异同步回文档。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 skill 简介。
- `SKILL.md`：面向 AI 的 skill 协议入口。
- `scripts/`：实际执行脚本目录。

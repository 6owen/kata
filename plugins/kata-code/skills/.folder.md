<!--
[INPUT]: `/plugins/kata-code/skills` 下 skill 子目录变化。
[OUTPUT]: `kata-code` skills 层的局部架构摘要与子目录索引。
[POS]: 位于 `/plugins/kata-code/skills`，作为 `kata-code` 能力集合层锚点。

[PROTOCOL]:
1. 一旦 skill 子目录增删或职责变化，必须同步更新我。
2. 若 skill 层结构变化，需同步回写上层 plugin 目录说明与根 README 的插件能力列表。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-code/skills

这里汇总 `kata-code` 的所有一方 skill。
每个 skill 目录都至少应包含 `README.md`、`SKILL.md` 与必要的 `scripts/`。
新增或下线 skill 时，必须先更新这里的索引。

- `.folder.md`：本目录的局部索引与同步锚点。
- `express-modular/`：Express 模块化后端骨架 skill。
- `pnpm-monorepo/`：pnpm monorepo 初始化与迁移 skill。
- `project-toolchain/`：工具链接入与规范化 skill。
- `starter-react/`：Vite React 单应用模板初始化与结构迁移 skill。
- `tailwind-iconify/`：Tailwind v4 + Iconify 工作流 skill。

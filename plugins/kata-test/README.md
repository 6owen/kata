<!--
[INPUT]: `kata-test` plugin 的当前骨架状态与规划方向。
[OUTPUT]: 面向人类的 `kata-test` plugin 占位说明。
[POS]: 位于 /plugins/kata-test，作为测试 plugin 的入口文档。

[PROTOCOL]:
1. 一旦 plugin 状态、skill 规划或边界变化，必须同步更新此 Header。
2. 更新后必须上浮检查 /plugins/kata-test/.folder.md、/plugins/.folder.md 与 /README.md 的描述是否依然准确。
-->

# kata-test

`kata-test` 当前只保留 plugin 骨架，后续补充：

- 单元测试 workflow
- 集成测试 workflow
- E2E 测试 workflow

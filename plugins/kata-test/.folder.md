<!--
[INPUT]: `/plugins/kata-test` 内 plugin 文档、元数据与 skills 结构变更。
[OUTPUT]: `kata-test` plugin 的局部架构摘要与文件职责索引。
[POS]: 位于 `/plugins/kata-test`，作为测试 plugin 根目录锚点。

[PROTOCOL]:
1. 一旦本目录文件或子目录变化，必须同步更新我。
2. 若测试能力从骨架进入真实实现，需同步回写 README、`plugin.json`、上层 `/plugins/.folder.md` 与根 README。
-->

一旦我所属的文件夹有所变化，请更新我。

# /plugins/kata-test

这里承载测试 plugin 的根说明、发布元数据与 skills 骨架。
当前还没有实装测试 skill，但目录结构已经纳入统一治理。
未来启用测试能力时，必须把职责边界先补齐到这里。

- `.folder.md`：本目录的局部索引与同步锚点。
- `README.md`：面向人类的 plugin 骨架说明。
- `plugin.json`：JSON 例外文件；提供 `kata-test` 的发布元数据。
- `skills/`：测试 skill 集合目录，细节由子目录 `.folder.md` 继续描述。
